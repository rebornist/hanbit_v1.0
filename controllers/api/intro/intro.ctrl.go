package intro

import (
	"github.com/labstack/echo/v4"
)

func IntroView(c echo.Context) error {
	// db := c.Request().Context().Value("DB").(*gorm.DB)
	// logger := c.Request().Context().Value("LOG").(*logrus.Entry)
	return nil
}
